{
  "name": "pet_shop",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "local": "NODE_ENV=local nodemon -r dotenv/config ./app.js",
    "dev": "NODE_ENV=dev nodemon -r dotenv/config ./app.js",
    "start": "NODE_ENV=production node -r dotenv/config ./app.js",
    "staging": "NODE_ENV=staging node -r dotenv/config ./app.js",
    "test": "jest --forceExit",
    "lint": "npx eslint . --ext .js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.2",
    "eslint": "^7.1.0",
    "jest": "^25.1.0",
    "supertest": "^4.0.2"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "pg": "^8.2.1",
    "pg-hstore": "^2.3.3",
    "sequelize": "^6.29.0",
    "sequelize-cli": "^5.5.1"
  }
}
